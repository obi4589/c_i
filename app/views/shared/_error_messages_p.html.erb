<% if @philanthropist.errors.any? %>
  <div id="error_explanation">
    <div class="alert-alert-error">
      The form contains <%= pluralize(@philanthropist.errors.count, "error") %>.
    </div>
    <ul>
    <% @philanthropist.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>