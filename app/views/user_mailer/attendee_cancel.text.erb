<%= @user.name %> (<%=philanthropist_url(@user)%>) is no longer attending <%=@event.title%> (<%=event_url(@event)%>) on <%= @event.start_time.strftime('%B %-d, %Y')%>!

Click here (<%= attendees_event_url(@event)%>) to see all attendees.